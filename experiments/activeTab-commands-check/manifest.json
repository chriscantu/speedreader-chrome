{
  "manifest_version": 3,
  "name": "activeTab+commands check (#105 D10)",
  "version": "0.0.1",
  "description": "Reproducer for ADR 2026-05-22-sw-lifecycle-activation §Empirical Precondition. Verifies that chrome.commands invocation grants activeTab host access for chrome.scripting.executeScript without host_permissions.",
  "permissions": ["activeTab", "scripting"],
  "action": {
    "default_title": "D10 reproducer — click to exercise the action-gesture path (T2)"
  },
  "background": {
    "service_worker": "background.js",
    "type": "module"
  },
  "commands": {
    "_toggle_reader": {
      "suggested_key": {
        "default": "Ctrl+Shift+Y",
        "mac": "MacCtrl+Shift+Y"
      },
      "description": "Fire activeTab+commands reproducer"
    }
  }
}
